# frozen_string_literal: true

RSpec.describe Build::Your::Own::Webserver do
  it "has a version number" do
    expect(Build::Your::Own::Webserver::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
